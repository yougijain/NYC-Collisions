pandas==2.3.0        # CSV/JSON ingest, ETL, DataFrame work
streamlit==1.45.1    # dashboard & caching APIs
pydeck==0.9.1        # heatmap
pytest==8.4.0        # automated data tests
requests==2.31.0     # NYC Open Data (Socrata) API client
duckdb==1.1.3        # SQL engine over the Parquet dataset
pyarrow==21.0.0      # Parquet read/write (17.0.0 has no Python 3.13 wheel)
